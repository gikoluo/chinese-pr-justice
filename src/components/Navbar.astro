---
// Navbar component - shared across all pages
const currentPath = Astro.url.pathname;
---

<div class="announcement-bar" id="announcement-bar">
    <a href="/event" class="announcement-content">
        <span>Procession to Parliament Hill — March 13, 2026 — Ottawa, Ontario</span>
    </a>
    <button class="announcement-close" aria-label="Close announcement">&times;</button>
</div>

<nav class="navbar">
    <div class="container">
        <a href="/" class="nav-brand">PR Justice Now</a>
        <button class="nav-toggle" aria-label="Toggle navigation menu" aria-expanded="false">
            <span class="nav-toggle-bar"></span>
            <span class="nav-toggle-bar"></span>
            <span class="nav-toggle-bar"></span>
        </button>
        <div class="nav-menu">
            <a href="/#declaration" class:list={[{ active: currentPath === '/' }]}>Declaration</a>
            <a href="/#demands">Our Demands</a>
            <a href="/#stories">Stories</a>
            <a href="/event" class:list={[{ active: currentPath === '/event' || currentPath === '/event/' }]}>Event</a>
            <a href="/join" class:list={[{ active: currentPath === '/join' || currentPath === '/join/' }]}>Join Us</a>
        </div>
    </div>
</nav>

<script>
    // Mobile hamburger menu toggle
    const navToggle = document.querySelector('.nav-toggle');
    const navMenu = document.querySelector('.nav-menu');

    if (navToggle && navMenu) {
        navToggle.addEventListener('click', () => {
            const isOpen = navMenu.classList.toggle('open');
            navToggle.classList.toggle('active', isOpen);
            navToggle.setAttribute('aria-expanded', String(isOpen));
        });

        // Close menu when a nav link is clicked
        navMenu.querySelectorAll('a').forEach(link => {
            link.addEventListener('click', () => {
                navMenu.classList.remove('open');
                navToggle.classList.remove('active');
                navToggle.setAttribute('aria-expanded', 'false');
            });
        });
    }

    // Announcement bar functionality
    const announcementBar = document.getElementById('announcement-bar');
    const announcementClose = document.querySelector('.announcement-close');

    function closeAnnouncement() {
        if (announcementBar) {
            announcementBar.classList.add('hidden');
        }
    }

    // Close on button click
    if (announcementClose) {
        announcementClose.addEventListener('click', closeAnnouncement);
    }

    // Close on scroll
    let hasScrolled = false;
    window.addEventListener('scroll', () => {
        if (!hasScrolled && window.scrollY > 0) {
            hasScrolled = true;
            closeAnnouncement();
        }
    }, { passive: true });
</script>

<style>
    /* Announcement Bar */
    .announcement-bar {
        background: rgb(229, 25, 55);
        height: 30px;
        display: flex;
        align-items: center;
        justify-content: center;
        position: relative;
        transition: transform 0.3s ease, opacity 0.3s ease;
    }

    .announcement-bar.hidden {
        transform: translateY(-100%);
        opacity: 0;
        height: 0;
        overflow: hidden;
    }

    .announcement-content {
        color: #ffffff;
        font-size: 0.9rem;
        font-weight: 400;
        text-align: center;
        text-decoration: none;
        transition: opacity 0.2s;
    }

    .announcement-content:hover {
        opacity: 0.85;
    }

    .announcement-close {
        position: absolute;
        right: 12px;
        top: 50%;
        transform: translateY(-50%);
        background: transparent;
        border: none;
        color: #ffffff;
        font-size: 1.25rem;
        line-height: 1;
        cursor: pointer;
        padding: 0 4px;
        opacity: 0.8;
        transition: opacity 0.2s;
    }

    .announcement-close:hover {
        opacity: 1;
    }

    /* Navigation – hero theme: dark blue gradient, white text, red accent */
    .navbar {
        background: linear-gradient(145deg, var(--primary-color) 0%, var(--primary-dark) 100%);
        box-shadow: 0 2px 12px rgba(0, 0, 0, 0.25);
        position: sticky;
        top: 0;
        z-index: 1000;
        padding: 0.85rem 0;
        border-bottom: 2px solid rgba(255, 255, 255, 0.08);
    }

    .navbar :global(.container) {
        display: flex;
        justify-content: space-between;
        align-items: center;
    }

    .nav-brand {
        font-size: 1.25rem;
        font-weight: 800;
        color: #fff;
        text-decoration: none;
        letter-spacing: 0.02em;
        text-shadow: 0 1px 2px rgba(0, 0, 0, 0.2);
        transition: color 0.2s, opacity 0.2s;
    }

    .nav-brand:hover {
        color: rgba(255, 255, 255, 0.95);
        opacity: 0.95;
    }

    .nav-menu {
        display: flex;
        gap: 2rem;
    }

    .nav-menu a {
        color: rgba(255, 255, 255, 0.92);
        text-decoration: none;
        font-weight: 600;
        font-size: 0.9375rem;
        letter-spacing: 0.02em;
        transition: color 0.2s, text-shadow 0.2s;
    }

    .nav-menu a:hover {
        color: #fff;
        text-shadow: 0 0 0 1px rgba(255, 255, 255, 0.3);
    }

    .nav-menu a.active {
        color: #fff;
        border-bottom: 2px solid var(--red);
        padding-bottom: 2px;
    }

    /* Mobile hamburger – white bars on dark nav */
    .nav-toggle {
        display: none;
        flex-direction: column;
        justify-content: space-between;
        width: 22px;
        height: 16px;
        background: transparent;
        border: none;
        cursor: pointer;
        padding: 0;
        z-index: 1001;
    }

    .nav-toggle-bar {
        display: block;
        width: 100%;
        height: 2px;
        background-color: #fff;
        border-radius: 2px;
        transition: transform 0.3s ease, opacity 0.3s ease;
    }

    /* Hamburger to X animation when menu is open */
    .nav-toggle.active .nav-toggle-bar:nth-child(1) {
        transform: translateY(7px) rotate(45deg);
    }

    .nav-toggle.active .nav-toggle-bar:nth-child(2) {
        opacity: 0;
    }

    .nav-toggle.active .nav-toggle-bar:nth-child(3) {
        transform: translateY(-7px) rotate(-45deg);
    }

    /* Responsive */
    @media (max-width: 968px) {
        .nav-menu {
            flex-wrap: wrap;
            gap: 0.75rem;
            justify-content: center;
        }
    }

    @media (max-width: 640px) {
        .nav-brand {
            font-size: 1rem;
        }

        .nav-menu {
            font-size: 0.875rem;
        }
    }

    /* Mobile navigation (≤575px) */
    @media (max-width: 575px) {
        .navbar :global(.container) {
            flex-wrap: wrap;
        }

        .nav-toggle {
            display: flex;
        }

        .nav-menu {
            display: none;
            flex-direction: column;
            width: 100%;
            gap: 0;
            margin-top: 1rem;
            padding-top: 1rem;
            border-top: 1px solid rgba(255, 255, 255, 0.15);
        }

        .nav-menu.open {
            display: flex;
        }

        .nav-menu a {
            padding: 0.75rem 0;
            border-bottom: 1px solid rgba(255, 255, 255, 0.08);
        }

        .nav-menu a:last-child {
            border-bottom: none;
        }

        .nav-menu a:hover {
            color: #fff;
        }

        .nav-menu a.active {
            border-bottom: 2px solid var(--red);
            padding-bottom: 0.5rem;
        }
    }
</style>
